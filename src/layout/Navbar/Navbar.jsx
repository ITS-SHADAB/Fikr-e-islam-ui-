import React from "react";
import Header from "../Header/Header";

// Navbar is now unified with Header as ONE single connected header component.
// Re-exporting Header as default so any direct imports remain fully compatible.
export default function Navbar() {
  return null;
}
